{
  "name": "spoteando-costa-rica",
  "version": "0.1.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "check:conflicts": "node scripts/check-merge-conflicts.js",
    "fix:conflicts": "node scripts/check-merge-conflicts.js --fix",
    "prestart": "npm run check:conflicts",
    "preandroid": "npm run check:conflicts",
    "preios": "npm run check:conflicts",
    "preweb": "npm run check:conflicts",
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "expo": "^50.0.0",
    "react": "18.2.0",
    "react-native": "0.73.6",
    "expo-image-picker": "~15.0.7",
    "expo-location": "~17.0.1",
    "react-native-maps": "1.10.0",
    "firebase": "^10.14.1"
  }
}
